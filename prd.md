# 宝可梦主题随机奖励抽签系统 - 产品需求文档 (PRD)

---

## 1. 项目概述

### 1.1 背景与目标
为了激励孩子完成日常任务（如做家务、写作业、早睡等），家长通常会设置一些随机奖励以增加趣味性和期待感。本项目旨在开发一款**宝可梦主题的随机奖励抽签小游戏**。
该工具为单页网页应用（SPA），兼具高颜值和趣味交互。孩子在完成任务后通过精灵球抽取随机卡牌获得奖励；家长（开发者）可通过密码锁后台对奖励池（签池）和中奖记录（荣誉榜）进行增删和核销管理。

### 1.2 目标用户与角色
*   **孩子（玩家）**：主要交互者，通过趣味动画抽取奖励，查看自己的中奖成就，并在需要兑换时向家长出示。
*   **家长（开发者/管理员）**：系统的配置者与核销人。负责编辑和增减奖励、修改访问密码、在孩子使用奖励后进行批量勾选和密码验证核销（删除记录）。

---

## 2. 核心功能需求

### 2.1 抽签召唤系统（孩子端）
*   **3D 精灵球召唤台**：
    *   页面中央展示一个 3D 浮雕质感的精灵球（PokeBall），带呼吸发光的就绪指示灯。
    *   点击“掷出精灵球 抽奖！”后，精灵球开始剧烈晃动（模拟宝可梦捕获时的抖动），且指示灯快速闪烁红光。
    *   抖动结束后，精灵球播放“爆裂弹开”动画，中间放射金色强光。
*   **卡牌翻转展示（Wow Factor）**：
    *   精灵球消失后，以 3D 翻转放大的动画效果飞出一张“宝可梦幸运卡牌”（TCG卡牌风格）。
    *   卡牌背景带有彩虹激光偏光效果（Holographic）。
    *   卡牌展示区域动态加载 1-151 只第一代宝可梦的高清官方原画，配有该宝可梦鼓励孩子的专属中文对话。
    *   卡牌中央加粗展示本次中奖的奖励文本。
    *   弹出时全屏燃放彩色 Canvas 碎纸屑烟花。

### 2.2 荣誉记录与核销系统（家长/孩子端）
*   **荣誉榜列表**：
    *   展示所有“未核销”的抽签历史记录。
    *   格式标准为：“`YYYY年MM月DD日 HH:mm` 抽得 `[奖励内容]`”。
    *   若没有中奖记录，展示可爱的宝可梦空状态画面。
*   **批量勾选核销**：
    *   列表每一项左侧带复选框，支持整行点击和多选。
    *   提供“全选”与“取消全选”的快捷操作。
    *   勾选后，底部显示“核销选中 (X)”按钮。点击按钮触发**身份验证密码锁**。
    *   密码输入正确后，被勾选的行播放“向左滑出并渐隐”的消失动画，然后从历史列表中永久删除（代表奖励已被兑换核销）。

### 2.3 开发者控制台（家长端）
*   **入口保护**：点击右上角锁头图标进入，必须通过**身份验证密码锁**验证。
*   **奖励池（签池）配置 Tab**：
    *   提供多行文本编辑框（Textarea）。**每一行文本代表一个签**，签的总数量及内容根据家长输入的行数实时计算。
    *   支持随时增添、修改或删除。
    *   提供“保存奖励池”和“重置为默认奖池”按钮（一键恢复经典的宝可梦主题初始奖励）。
*   **系统安全设置 Tab**：
    *   支持家长修改身份验证密码。
    *   修改时需输入“新密码”和“确认新密码”，确保一致且不小于 4 位。

### 2.4 家长身份验证密码锁（安全控制）
*   *防作弊机制*：防止孩子自己偷偷核销已获得的奖励或恶意篡改奖池配置。
*   提供一个大号圆润的**数字软键盘（PIN Keypad）**，方便在平板和手机上点按输入。
*   默认初始密码为 **`1234`**。

---

## 3. 非功能性需求

### 3.1 数据持久化
*   所有核心数据均保存在客户端浏览器中，采用 `window.localStorage` 进行读写：
    *   `pokemon_rewards`：保存奖励池列表（纯文本以换行符分隔）。
    *   `pokemon_records`：保存中奖历史的 JSON 数组。
    *   `pokemon_pin`：保存身份验证密码。
*   只要不主动清除浏览器缓存或强制重置，数据长久有效，支持离线使用。

### 3.2 界面与视觉规范 (Premium UI)
*   **色调**：以宝可梦红（`#ff1c1c`）、皮卡丘黄（`#fcd705`）、深邃极简蓝紫渐变背景为主，具有现代动漫科技感。
*   **UI质感**：采用毛玻璃卡片设计（Glassmorphism）、微小高光边框、多层渐变立体阴影。
*   **响应式布局**：
    *   **PC 端**：左右分栏布局（左侧抽奖召唤，右侧荣誉榜记录）。
    *   **移动端（手机/iPad）**：自动折叠为上下单栏布局，优化大按钮触控范围。

### 3.3 性能与加载
*   宝可梦卡牌角色原画优先从公开 CDN/API（PokeAPI）实时获取。
*   在图片加载完成前，卡牌需显示 Loading 提示动画；若网络异常或无网，系统自动切换为默认备用图片，确保核心抽奖功能可用。

---

## 4. 数据模型设计

### 4.1 中奖记录 (Records Schema)
```typescript
interface DrawRecord {
    id: number;     // 唯一时间戳 ID (如 Date.now())
    time: string;   // 格式化时间字符串 "YYYY年MM月DD日 HH:mm"
    reward: string; // 抽中的奖励内容文本
}
```

### 4.2 密码数据 (PIN Schema)
```typescript
type PokemonPIN = string; // 最多 10 位的数字字符串，默认为 "1234"
```

---

## 5. 项目未来规划 (Roadmap)
*   **声效系统**：加入精灵球掷出、收回、宝可梦叫声等经典游戏音效。
*   **数据导出/导入**：支持将中奖记录或奖池配置以 JSON 或文本形式导出到微信/备忘录备份，防误删。
*   **任务卡点系统**：引入每日签到或打卡计数，孩子每获得一定积分自动兑换一次抽奖机会。
